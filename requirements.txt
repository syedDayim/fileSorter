# Advanced File Sorter Requirements
# All dependencies are built-in Python modules, no external packages required

# Built-in modules used:
# - tkinter (GUI framework)
# - os (file system operations)
# - shutil (file operations)
# - pathlib (path handling)
# - threading (background operations)
# - time (timing)
# - datetime (timestamping)
# - json (settings storage)

# Optional: For enhanced functionality, you could install:
# - pillow (for image previews): pip install pillow
# - psutil (for system monitoring): pip install psutil
